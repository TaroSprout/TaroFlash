name: CI

on:
  pull_request:
  push:
    branches: [master]

jobs:
  changes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
      functions: ${{ steps.filter.outputs.functions }}
      db: ${{ steps.filter.outputs.db }}
      contract: ${{ steps.filter.outputs.contract }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            frontend:
              - 'src/**'
              - 'tests/**'
              - 'types/**'
              - 'public/**'
              - 'index.html'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'tsconfig*.json'
              - 'vite.config.*'
              - 'vitest.config.*'
              - '.oxlintrc*'
              - 'oxlint.json'
            functions:
              - 'supabase/functions/**'
            db:
              - 'supabase/migrations/**'
              - 'supabase/schemas/**'
              - 'supabase/tests/**'
              - 'supabase/seed.sql'
              - 'supabase/config.toml'
            contract:
              - 'src/api/**'
              - 'tests/contract/**'
              - 'tests/fixtures/**'
              - 'types/**'
              - 'supabase/migrations/**'
              - 'supabase/seed.sql'
              - 'supabase/config.toml'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'vite.config.*'

  lint:
    needs: changes
    if: needs.changes.outputs.frontend == 'true' || github.event_name == 'push'
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: ./.github/actions/setup-node

      - name: Run lint
        run: pnpm exec vp lint

      - name: Run type-check
        run: pnpm type-check

  test:
    needs: changes
    if: needs.changes.outputs.frontend == 'true' || github.event_name == 'push'
    runs-on: ubuntu-latest

    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: ./.github/actions/setup-node

      - name: Install Playwright Chromium
        run: pnpm exec playwright install chromium

      - name: Run tests (shard ${{ matrix.shard }}/3)
        run: pnpm exec vp test run --coverage --reporter=blob --reporter=default --project Unit --project Integration --shard=${{ matrix.shard }}/3

      - name: Upload blob report
        uses: actions/upload-artifact@v4
        with:
          name: blob-unit-integration-${{ matrix.shard }}
          path: .vitest-reports/
          retention-days: 30
          include-hidden-files: true

  test-functions:
    needs: changes
    if: needs.changes.outputs.functions == 'true'
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Install function dependencies
        working-directory: supabase/functions
        run: deno install

      - name: Run edge function tests
        working-directory: supabase/functions
        run: deno test --allow-net --allow-env --allow-read

  test-db:
    needs: changes
    if: needs.changes.outputs.db == 'true'
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start Supabase
        run: supabase start -x realtime,storage-api,imgproxy,inbucket,postgrest-api,pgadmin-schema-diff,migra,postgres-meta,studio,edge-runtime,logflare,vector,supavisor

      - name: Run database tests
        run: supabase test db

      # supabase/schemas/ must stay in sync with the migration history: a
      # schema edit without a generated migration (or a hand-written DDL
      # migration without a schema edit) makes db diff non-empty, which
      # writes a migration file here and fails the build.
      - name: Check declarative schema sync
        run: |
          supabase db diff -f ci_drift_check
          drift=$(find supabase/migrations -name '*ci_drift_check.sql')
          if [ -n "$drift" ]; then
            echo "::error::supabase/schemas is out of sync with migrations — run 'supabase db diff -f <name>' locally and commit the result. Drift:"
            cat $drift
            exit 1
          fi

  test-contract:
    needs: changes
    if: needs.changes.outputs.contract == 'true' || github.event_name == 'push'
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: ./.github/actions/setup-node

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start Supabase
        run: supabase start -x realtime,imgproxy,inbucket,pgadmin-schema-diff,migra,postgres-meta,studio,edge-runtime,logflare,vector,supavisor

      - name: Export local Supabase keys
        run: |
          {
            echo "SUPABASE_URL=$(supabase status -o env | grep ^API_URL | cut -d= -f2- | tr -d '\"')"
            echo "SUPABASE_ANON_KEY=$(supabase status -o env | grep ^ANON_KEY | cut -d= -f2- | tr -d '\"')"
            echo "SUPABASE_SERVICE_ROLE_KEY=$(supabase status -o env | grep ^SERVICE_ROLE_KEY | cut -d= -f2- | tr -d '\"')"
          } >> "$GITHUB_ENV"

      - name: Run contract tests
        run: pnpm exec vp test run --coverage --reporter=blob --reporter=default --project Contract

      - name: Rename blob to avoid collision with unit blob
        run: mv .vitest-reports/blob.json .vitest-reports/blob-contract.json

      - name: Upload blob report
        uses: actions/upload-artifact@v4
        with:
          name: blob-contract
          path: .vitest-reports/
          retention-days: 30
          include-hidden-files: true

  coverage-report:
    needs: [changes, lint, test, test-contract]
    if: |
      always() &&
      needs.lint.result == 'success' &&
      needs.test.result == 'success' &&
      (needs.test-contract.result == 'success' || needs.test-contract.result == 'skipped')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: ./.github/actions/setup-node

      - name: Download unit/integration blobs (all shards)
        uses: actions/download-artifact@v4
        with:
          pattern: blob-unit-integration-*
          path: .vitest-reports/
          merge-multiple: true

      - name: Download contract blob (this run)
        if: needs.test-contract.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: blob-contract
          path: .vitest-reports/

      - name: Download contract blob (master fallback)
        if: needs.test-contract.result == 'skipped'
        uses: dawidd6/action-download-artifact@v6
        continue-on-error: true
        with:
          workflow: ci.yml
          workflow_conclusion: success
          branch: master
          name: blob-contract
          path: .vitest-reports/
          if_no_artifact_found: warn

      # Master's contract blob carries stale v8 instrumentation for files
      # outside src/api/ (transitively imported during contract tests).
      # When merged against PR source where those files have changed line
      # numbers, v8 misaligns and reports drops on otherwise well-covered
      # files (e.g. src/sfx/*). Strip non-api entries so the fallback blob
      # contributes only its actual value: api coverage.
      - name: Strip non-api entries from stale contract blob
        if: needs.test-contract.result == 'skipped'
        run: |
          python3 - <<'PY'
          import json, pathlib
          path = pathlib.Path('.vitest-reports/blob-contract.json')
          if not path.exists():
              raise SystemExit(0)
          blob = json.loads(path.read_text())

          def is_strip(key: str) -> bool:
              return '/src/' in key and '/src/api/' not in key

          counter = [0]

          def visit(node):
              if isinstance(node, dict):
                  for k in list(node.keys()):
                      if isinstance(k, str) and is_strip(k):
                          del node[k]
                          counter[0] += 1
                      else:
                          visit(node[k])
              elif isinstance(node, list):
                  for item in node:
                      visit(item)

          visit(blob)
          path.write_text(json.dumps(blob))
          print(f'stripped {counter[0]} non-api file entries from stale contract blob')
          PY

      - name: Merge coverage
        id: merge
        continue-on-error: true
        run: pnpm exec vp test --merge-reports --coverage

      # `vp test --merge-reports --coverage` occasionally exits non-zero even
      # after successfully writing coverage-summary.json (e.g. Vue warns
      # flipping an internal error count, or mixed vitest+coverage-v8
      # versions). Check the file landed on disk instead of trusting the
      # step's exit code.
      - name: Check merged summary present
        id: merged
        run: |
          if [ -f coverage/coverage-summary.json ]; then
            echo "ok=true" >> "$GITHUB_OUTPUT"
          else
            echo "ok=false" >> "$GITHUB_OUTPUT"
            echo "::error::Merge coverage did not produce coverage/coverage-summary.json"
            exit 1
          fi

      - name: Report Coverage
        if: github.event_name == 'pull_request'
        uses: davelosert/vitest-coverage-report-action@v2
        with:
          file-coverage-mode: 'none'
